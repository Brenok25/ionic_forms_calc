import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic_forms_calc',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
